apiVersion: v2
name: platform
description: Enterprise communication platform — backend, workers, scheduler, frontend.
type: application
version: 0.1.0
appVersion: "1.0.0"
kubeVersion: ">=1.24.0-0"
maintainers:
  - name: Platform Team