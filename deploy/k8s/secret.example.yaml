# Template only. Never commit real values. Use sealed-secrets / external-secrets in production.
apiVersion: v1
kind: Secret
metadata:
  name: platform-secrets
  namespace: platform
type: Opaque
stringData:
  DATABASE_URL: "postgresql+psycopg2://user:password@postgres:5432/platform"
  REDIS_URL: "redis://redis:6379/0"
  APP_SECRET_KEY: "replace-me-32-plus-chars"
  WEBHOOK_SECRET: "replace-me"