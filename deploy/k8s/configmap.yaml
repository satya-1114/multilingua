apiVersion: v1
kind: ConfigMap
metadata:
  name: platform-config
  namespace: platform
data:
  APP_ENV: "production"
  LOG_LEVEL: "INFO"
  CORS_ORIGINS: "https://app.example.com"
  WORKER_CONCURRENCY: "4"
  RATE_LIMIT_ENABLED: "true"